{
  "name": "smart-on-fhir-tests",
  "version": "1.0.0",
  "description": "Tests for SMART on FHIR implementation",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:backend-services": "jest smart-flows/backend-services",
    "test:ehr-launch": "jest smart-flows/ehr-launch",
    "test:standalone-launch": "jest smart-flows/standalone-launch",
    "test:client-registration": "jest client-registration",
    "test:smart-flows": "jest smart-flows",
    "test:oauth-security": "jest oauth-security",
    "test:grant-types": "jest oauth-security/grant-types",
    "test:credential-leakage": "jest oauth-security/credential-leakage",
    "test:token-security": "jest oauth-security/token-security",
    "generate-api": "bunx @openapitools/openapi-generator-cli generate -i http://localhost:8445/swagger/json -g typescript-fetch -o ./lib/api-client --skip-validate-spec",
    "validate-api": "bunx @openapitools/openapi-generator-cli validate -i http://localhost:8445/swagger/json"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.7.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "@types/supertest": "^6.0.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "fhirclient": "^2.6.0",
    "jose": "^5.0.0"
  }
}
